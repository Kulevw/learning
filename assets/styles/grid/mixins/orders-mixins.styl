make_orders(count, bp = '')
  +c('order', bp, 'first')
    order -1
    padding 0

  for span in (0 .. count)
    +c('order', bp, span)
      order span

  +c('order', bp, 'last')
    order count + 1
